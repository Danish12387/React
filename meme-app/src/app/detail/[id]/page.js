'use client';
import React from 'react'

const Detail = (props) => {
  // const { name, url, width, height, } = data;
  console.log(props);

  return (
    <div>
      {/* <h1>{name}</h1>
      <img src={url} /> */}
    </div>
  )
}

export default Detail;
